<Page
    x:Class="UniversalSend.Views.ReceivePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalSend.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:UniversalSend.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="24">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Height" Value="40"/>
                </Style>
            </StackPanel.Resources>
            <Button>
                <FontIcon Glyph="&#xE81C;"/>
            </Button>
            <Button>
                <FontIcon Glyph="&#xF167;"/>
            </Button>
        </StackPanel>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" Text="Laptop" Style="{ThemeResource HeaderTextBlockStyle}"/>
        <TextBlock Grid.Row="2" HorizontalAlignment="Center" Text="#101" Style="{ThemeResource TitleTextBlockStyle}"/>
        <controls:QuickSaveOptionsControl Grid.Row="3" x:Name="QuickSaveOptionsControl"/>
    </Grid>
</Page>
