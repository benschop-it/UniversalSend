<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Theme-aware color palette -->
    <ResourceDictionary.ThemeDictionaries>
        <ResourceDictionary x:Key="Light">
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="Black" />
            <SolidColorBrush x:Key="SecondaryTextBrush" Color="White" />
            <SolidColorBrush x:Key="IconForegroundBrush" Color="Black" />

            <SolidColorBrush x:Key="AcceptButtonBrush" Color="#FF4C5B92" />
            <SolidColorBrush x:Key="DeclineButtonBrush" Color="#FFBA1A1A" />
            <SolidColorBrush x:Key="TagBackgroundBrush" Color="#FF151B2B" />
        </ResourceDictionary>

        <ResourceDictionary x:Key="Dark">
            <SolidColorBrush x:Key="PrimaryTextBrush" Color="White" />
            <SolidColorBrush x:Key="SecondaryTextBrush" Color="Black" />
            <SolidColorBrush x:Key="IconForegroundBrush" Color="White" />

            <SolidColorBrush x:Key="AcceptButtonBrush" Color="#FFB5C4FF" />
            <SolidColorBrush x:Key="DeclineButtonBrush" Color="#FFFEB4AB" />
            <SolidColorBrush x:Key="TagBackgroundBrush" Color="#FFDDE1FA" />
        </ResourceDictionary>
    </ResourceDictionary.ThemeDictionaries>

    <!-- Reusable Rounded Button Style -->
    <Style x:Key="RoundedButtonStyle" TargetType="Button">
        <Setter Property="Foreground" Value="{ThemeResource PrimaryTextBrush}" />
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="14,8" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="20"
                            HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalAlignment}"
                            Margin="{TemplateBinding Margin}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
