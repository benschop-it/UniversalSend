<UserControl
    x:Class="UniversalSend.Controls.SendPage.SendItemButtonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalSend.Controls.SendPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Button x:Name="RootButton" x:FieldModifier="public" Margin="4" Width="80" Height="60">
        <StackPanel>
            <FontIcon Glyph="{x:Bind IconGlyph,Mode=OneWay}"/>
            <TextBlock HorizontalTextAlignment="Center" Text="{x:Bind Label,Mode=OneWay}"/>
        </StackPanel>
    </Button>
</UserControl>
