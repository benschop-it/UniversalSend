<UserControl
    x:Class="UniversalSend.Controls.ContentDialogControls.RenameStorageItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalSend.Controls.ContentDialogControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="重命名"/>
        <TextBox Grid.Row="1" Name="NameTextBox" x:FieldModifier="public" VerticalAlignment="Top" HorizontalAlignment="Stretch"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button Name="CancelButton" Content="取消" x:FieldModifier="public" Margin="0,0,4,0"/>
            <Button Name="OKButton" Content="确认" x:FieldModifier="public"/>
        </StackPanel>
    </Grid>
</UserControl>
