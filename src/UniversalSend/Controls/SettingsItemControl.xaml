<UserControl
    x:Class="UniversalSend.Controls.SettingsItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalSend.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Loaded="UserControl_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Name="HeaderTestBlock" Margin="0,0,0,4" Text="{x:Bind Header,Mode=OneWay}" Style="{ThemeResource BodyTextBlockStyle}"/>
        <Grid Grid.Row="1" Name="SettingControlGrid" Margin="0,0,0,8">
            
        </Grid>
    </Grid>
</UserControl>
